import os
from time import sleep
from procedures.procedures import shopping

shopping.First_section()

    # def show_product_bought(self, choice):
    #     prc = list_values[choice - 1]
    #     crc = list(inchoice)[choice-1]
    #     print(f"You have bought {crc} for ${prc}")
  
            
